---
description: 'Quick wireframe từ BRD/idea → code screens + states browser → localhost. Trigger: "wireframe", "wf", "sketch", "phác", "mock"'
---

$ARGUMENTS

Load and follow the skill at `.claude/skills/wireframe/SKILL.md` exactly.

CRITICAL: States browser MUST copy the full template pattern from `app/sinhloi/page.tsx` — 2 tabs (UI + Flow), sidebar, iPhone frame, Mermaid charts. NEVER create a simplified version.

Input: `$ARGUMENTS`
